#ifndef RAINING_HPP
#define RAINING_HPP

#include <string>
#include <vector>

// Prints the current businesses
void printBusinesses(const std::vector<std::string>& businesses);

// Returns true if user input means "yes"
bool wantsAnother(const std::string& response);

#endif
